/**
 * LeTable 表格组件
 * 基于 NDataTable + NPagination 的二次封装
 * 支持自动序号列、操作列
 */
export { Table, default } from './src/Table';
export type { TableProps, TablePagination, OperateColumnConfig } from './src/Table';
